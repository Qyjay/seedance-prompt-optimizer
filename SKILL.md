---
name: seedance-prompt-optimizer
description: 把杂乱、不清晰的视频生成 prompt 改写为符合 Seedance 2.0（豆包多模态视频生成模型）最佳实践的结构化 prompt。基于火山引擎官方 PE 指南融合实测：U 型注意力机制（首尾权重最高）、八要素万能公式（精准主体+动作细节+场景环境+光影色调+镜头运镜+视觉风格+画质参数+约束条件）、剧组编排（角色锚定/场景定调/运镜参考/节奏氛围）、时间轴化分镜脚本、良品率五维衡量标准（ID 稳定/画面稳定/镜头合规/风格约束/多模态绑定）。规范多模态指代（[图N]/[视频N]/[音频N]），明确任务类型（参考/编辑/延长/组合），主动规避 V-1~V-18、A-1~A-4 等已知陷阱（人物 ID 漂移、字幕/水印、风格漂移、双胞胎、胡言乱语、结尾噪音、中文发音不准、第一人称漂回第三人称、九宫格失效、时间戳 HUD 不稳、多人歧义断句、数字滚动、背景文字招牌等）。内置第一人称视角（POV / FPV / Orb）专章。当用户的 prompt 含混冗长、混合中英、堆砌多种素材、出现"视频生成 / 抖音视频 / Seedance / 豆包视频 / 即梦视频 / 提示词改写 / prompt 优化 / 第一人称 / POV / FPV / 主观镜头 / GoPro / 行车记录仪"等信号词，或视频出现典型症状需要诊断改写时，调用本 skill。
---

# Seedance 2.0 Prompt Optimizer

把用户口语化、堆砌的视频 prompt，改写为 **Seedance 2.0** 能稳定执行的结构化指令。

> 方法论来源：火山引擎官方《Seedance 2.0 - PE 指南》+ 公网实测（VIDEO AI ME / Higgsfield）+ 客户实战沉淀。

## 调用判断（先做这一步）

满足以下任一即应调用：

1. 用户明说"帮我写 / 优化 Seedance / 豆包 / 即梦视频提示词"。
2. 用户给了一段视频 prompt 草稿，让你"润色 / 改写 / 重写 / 再写一遍"。
3. 用户描述视频生成的典型症状（ID 漂移、撞明星脸、字幕乱出、出现 B 站/芒果 logo、风格突然写实、双胞胎、台词胡言乱语、中文发音错、结尾咔哒声、第一人称漂回第三人称、九宫格参考无效、时间戳 HUD 不稳、多人数量出错、背景招牌文字漂移、数字滚动跳动等），并希望修复。
4. 用户给了多个素材（图片 / 视频 / 音频），想"组合生成一段视频"。
5. 用户出现"第一人称 / POV / FPV / 主观镜头 / GoPro / 行车记录仪 / 沉浸视角 / 玩家视角"等信号词 → 直接跳到 `references/first-person-pov.md` 套用六要素骨架与 Recipe。

不调用：纯文案改写、PPT 文案、营销 slogan 等与视频生成无关的任务。

## 良品率定义（输出质量的客观标尺）

输出 prompt 的目标不是"看起来很专业"，而是让最终生成视频在以下五维全部达标。详见 `references/quality-rubric.md`。

| 维度 | 含义 |
|---|---|
| **角色一致性（ID Stability）** | 多镜头切换时，面部、体型、服饰无漂移、不变脸 |
| **画面稳定性（Frame & Motion Stability）** | 无大幅抖动、无规律闪烁、无跳帧、动作自然不僵硬不穿模 |
| **镜头语言符合度（Camera Language Compliance）** | 景别 + 运镜与提示词指令精确匹配 |
| **风格与约束执行度（Style & Constraint Adherence）** | 风格、光影、画质、所有负向约束被严格执行 |
| **多模态绑定准确性（Multi-modal Binding Accuracy）** | 每个 [图N]/[视频N]/[音频N] 的角色（锚定/定调/运镜/音色）被准确执行 |

## 全局硬规则（任何输出都必须遵守）

1. **U 型注意力机制（最关键）**。Seedance 2.0 对 prompt 的注意力呈 **U 型**：开头与结尾权重最高，中间最低。强制要求：
   - **开头放核心指令**：主体定义 + 关键参考素材（如 `[图1]为角色"X"的外观参考`）+ 风格关键词。
   - **结尾放全局约束**：画质参数（4K 超高清、60fps、HDR）+ 强制性约束（面部稳定不变形、无字幕、无 logo、无水印）+ 时长比例参数。
   - **中间放叙事/动作**：分镜剧情、对话、运镜细节。
2. **任务类型必须明确**。先判定是 **参考 / 编辑 / 延长 / 组合** 中的哪一种，再选句式。在 **编辑 / 延长** 任务里**不要**出现"参考视频 N"，直接写"[视频 N]"。
3. **主体必须显式定义**。涉及任何参考素材中的角色 / 道具 / 场景，第一次出现就用以下句式定义：
   - **角色外观**：`[图N]为角色"角色名"的外观参考,严格还原[特征列表]`。示例：`[图2]为角色"爱豆"的外观参考,严格还原棕色毛绒质感、大眼圆脸、红色背带短裤+白T恤+橙色帆布鞋。`
   - **道具/场景**：`[图N]为关键道具"道具名"参考,严格还原[特征列表]`。示例：`[图4]为关键道具"嗨皮表"参考,严格还原红色方形表盘、红色硅胶表带、屏显10:08。`
   - **音频音色绑定**：`[音频N]为角色"角色名"的专属音色参考（音色特征描述，如：憨厚带点鼻音的男童声），本视频中所有由角色名发出的台词,必须严格使用[音频N]的音色合成,禁止用于其他任何角色。` —— **音色特征描述不可省略**，必须结合用户上传的角色参考图与参考音频，在提示词里显式给出简短具体的音色描述；至少包含性别、年龄感、2–3 个声音质感词（如"声音低沉""鼻音重""稚嫩透亮""憨厚带鼻音""温柔偏低沉"）。即使用户已经上传参考音频，也必须补充文字音色锚点；参考音频质量不足或声音很有特色时，文字描述是模型二次校准的关键依据。
   - 之后每次提到该主体都要重复角色名，**禁止省略指代**。
4. **多模态指代统一格式**：图片用 `[图1]/[图2]…`，视频用 `[视频1]/[视频2]…`，音频用 `[音频1]/[音频2]…`。Asset 库 ID **不能**直接替代 `[图1]`。**素材按 prompt 中出场顺序上传**（A-3 串台修复关键）。
5. **台词格式按规范使用**：
   - **画内台词**（角色在镜头内说话）：`角色名(画内,音色严格使用[音频N],禁止偏离[音频N]):{台词内容}`。示例：`爱豆(画内,音色严格使用[音频1],禁止偏离[音频1]):{走呀!}`
   - **画外音**（角色不在镜头内）：`角色名(音色描述,画外):{台词内容}`。示例：`妈妈(30岁温柔女声,模型合成,不使用任何参考音频,画外):{去超市帮我买菜!}`
   - **无参考音频的角色**须在括号内注明"模型合成,不使用任何参考音频"。
   - 其他特殊字符：音乐 `()`、音效 `<>`、字幕 `【】`；台词为小语种必须标注语种。
6. **参数中文化**：在 prompt 头/尾建议加 `时长：4秒，比例：9:16` —— 必须用"秒"，不能写"s"。当前模型仅支持 **4–15 秒**；想要 < 4 秒的成片，先按 4 秒生成再剪辑裁切。
7. **时序写法（PE 指南推荐）**：分镜建议使用"**镜头 1（0 至 5 秒）/ 镜头 2（5 至 10 秒）**"这种**段级时间轴**，模型对此理解稳定。⛔ **不要**在单一动作里写"0.3 秒后""1.7 秒时"这种**亚秒级精确时刻**。
8. **避免符号噪声**：不要在同一句中大量混用 `--` `~` 这类符号；不要用箭头 `→` 串接 beat（模型可能渲染成画面元素）。用自然语言描述时序与因果即可。
9. **默认开启反字幕、反 logo、反双胞胎、反变形护栏**（除非用户明确要求保留）。结尾约束至少包含：
   - `保持无字幕，避免画面生成字幕`
   - `不要生成 logo，不要生成水印`
   - `视频全程不要在同一画面中复制相同人物，不要多人同脸`
   - `面部稳定不变形，五官清晰，人体结构正常，动作自然流畅不僵硬，无卡顿，无闪烁`
10. **横屏优先**：当用户没指定比例且需要文字 / 多镜头 / 多角色时，主动建议 **16:9**（PE 指南实测：竖屏字幕和胡言乱语率显著更高）。
11. **画质参数模板**（默认追加到 prompt 结尾）：`4K 超高清、60fps 高帧率、HDR、细节丰富、无噪点、无模糊、电影质感`。
12. **拒绝模糊词**：删除"好看的""高级的""有感觉""氛围感""电影感"这类无法量化描述。"电影感"必须拆解为"逆光 + 金色轮廓光 + 暖饱和色调 + 浅景深 + 35mm 胶片颗粒"等可视参数。

## 五步优化流程（每次都按这个顺序走）

### Step 1 · 解析意图

读用户原 prompt + 附带素材清单，回答：

- 想要的最终画面是什么（一句话总结）？
- 用户提供了几张图、几段视频、几段音频？分别承担"剧组"中的哪类角色（**角色锚定 / 场景定调 / 运镜参考 / 节奏氛围**）？详见 `references/director-method.md`。
- 是否有典型症状描述（ID 漂移 / 字幕 / 双胞胎 / 风格漂移 / 胡言乱语 / 结尾噪音 / 中文发音不准 / 九宫格无效 / 时间戳 HUD 不稳 / 多人数量错 / 背景招牌漂移 / 数字滚动跳动等）？
- 时长（4–15 秒区间）、比例、画面风格是否明确？

### Step 2 · 选择任务类型

| 用户诉求 | 任务类型 | 推荐句式 |
|---|---|---|
| 借素材里的某个元素生成全新视频 | **参考（Reference）** | "参考 [图N] 中的「主体N」，生成…" / "参考 [视频N] 的[动作/运镜/风格/音效]，生成…" |
| 在原视频上加 / 删 / 改一个元素 | **编辑（Edit）** | "严格编辑 [视频N]，将其中的[原特征]修改为[新特征]…"（**不写"参考视频N"**） |
| 续写或前补一段视频 | **延长（Extend）** | "延长 [视频N]，生成…" / "向前延长 [视频N]，生成…"（**不写"参考视频N"**） |
| 既要参考一个素材，又要编辑另一个素材 | **组合** | "参考 [图N] 的[参考维度]，严格编辑 [视频X]，[具体编辑内容]" |

详见 `references/task-types.md`。

### Step 3 · 套用八要素万能公式（PE 指南权威版本）

按以下槽位组装；非必须项可省，但出现的话必须按这个顺序写：

```
精准主体 + 动作细节 + 场景环境 + 光影色调 + 镜头运镜 + 视觉风格 + 画质参数 + 约束条件
```

配合 U 型注意力机制布局：

- **首部（开头权重区）**：素材定义 + 精准主体（含角色名、外观）+ 关键参考指代。
- **中部（叙事区）**：按"镜头 1 / 镜头 2 / 镜头 3"分镜，每镜内嵌动作 + 场景 + 光影 + 运镜。
- **尾部（结尾权重区）**：视觉风格 + 画质参数（4K/60fps/HDR）+ 约束条件（反字幕/反 logo/反双胞胎/反变形）+ 时长比例。

详细字段、词典、范例见 `references/core-formula.md` 和 `references/camera-language.md`。

> 备注：原"六元素"拆分（镜头/主体/动作/@素材/风格/音频/限制）作为内部组装节奏使用，对外说明时统一用"八要素万能公式"。

### Step 4 · 体检 18 个陷阱

按 `references/pitfalls-visual.md`（V-1 ~ V-18）+ `references/pitfalls-audio.md`（A-1 ~ A-4 + F-1）逐条对照，若命中则就地补救。常见三连击：

- **加反字幕约束**：业务允许的话同时建议改横屏。
- **加反 logo / 反水印约束**。
- **加反双胞胎约束**（涉及单人参考时）。
- **加反变形约束**（人像必加）：面部稳定不变形、五官清晰、人体结构正常。

**专项修复路径**：

- **ID 漂移（V-1）** → 把人脸单独裁剪为独立大头照，放在 prompt 前部最高优先级，并明确"「主体N」的面部特征参考 [图1]（大头照），妆造参考 [图2]（全身照）"。**禁止建议三视图 / 多视图**。
- **中文发音错（A-2）** → 用同音常用字替换（"棪木" → "燕木"），或建议先用 TTS 生成音频转黑屏视频再作参考。
- **第一人称 / POV / FPV / 主观镜头** → 跳到 `references/first-person-pov.md`，**不要**单独写"第一人称视角"，必须套用六要素骨架（命名设备 + 肢体锚点 + 三个环境细节 + 按 beat 动作 + 命名光源 + 反向约束）。详见 V-12。
- **九宫格参考无效（V-13）** → 拆分九宫格为单图分别引用。
- **时间戳/HUD 不稳（V-14）** → 明确位置 + 颜色 + 格式 + "整个视频期间稳定叠加，无闪烁"。
- **多人歧义断句（V-15）** → 把"美女3 美女4"改成"美女C 美女D"等无歧义命名。
- **背景招牌文字（V-16）** → 提供清晰参考图，prompt 写"背景招牌文字参考 [图N]"；不需要时显式"背景不要出现任何文字"。
- **数字滚动 / 倒计时（V-17）** → 必须提供参考视频，写"数字滚动节奏和方向参考 [视频N]"。
- **多人物 / 多素材（V-18）** → 单次生成人物 ≤ 4，参考素材总量 ≤ 4–5；超出建议分镜拆段或先生成图再合成视频。
- **延长跳变 / 画质劣化** → 后期剪辑前段去尾 6 帧、后段去首 1 帧；或用 Seedance 转白模视频再作输入。

### Step 5 · 输出结构化 Prompt

最终回复严格按以下三段式：

```
🎯 诊断
- 任务类型：xxx
- 命中陷阱：V-x / A-x（若无则写"无"）
- 五维体检：ID稳定 / 画面稳定 / 镜头合规 / 风格约束 / 多模态绑定 各维度风险点（可省）
- 关键修改点：3 条以内

✅ 优化后的 Prompt
（一段可直接复制粘贴的自然语言 prompt，遵循 U 型布局：开头核心指令 + 中间分镜剧情 + 结尾全局约束 + 时长比例）

📌 使用说明
- 素材输入顺序：图片1=xxx，图片2=xxx，视频1=xxx，音频1=xxx（必须按 prompt 出场顺序）
- 时长：x秒（4–15 秒区间），比例：x:x
- 抽卡建议：例如"如仍出字幕，改为 16:9 重抽 1–2 次"
- 终极兜底：抽卡 5 次仍未达标 → 走 references/escalation.md 联系 CSM
```

## 一页纸自查清单（每次输出前过一遍）

详见 `references/checklist.md`，核心 4 段：

1. **准备阶段**：核心目标、参考素材角色分配是否清晰
2. **八要素阶段**：精准主体/动作/场景/光影/运镜/风格/画质/约束 是否齐备
3. **多模态结构**：[图N] 指代是否明确、分镜结构是否用"镜头 1/2/3"；如有角色参考音频，是否已根据角色参考图和参考音频写出简短具体音色描述
4. **最终审查**：模糊词是否清除、U 型布局首尾是否到位、通读是否无歧义

## 参考材料索引

阅读这些文件来获取细节，按需取用：

- `references/quality-rubric.md` —— **良品率五维衡量标准**：ID 稳定 / 画面稳定 / 镜头合规 / 风格约束 / 多模态绑定。
- `references/director-method.md` —— **导演级方法论**：剧组编排（四类角色）+ 时间轴化分镜脚本 + 提示词扩写润色（LLM/VLM 辅助）。
- `references/checklist.md` —— **一页纸提示词写作自查清单**（PE 指南权威版）。
- `references/escalation.md` —— **终极手段**：抽卡失败时联系火山引擎 CSM 的标准操作（提供完整 prompt + 素材 + request_id）。
- `references/task-types.md` —— 参考 / 编辑 / 延长 / 组合 四种任务的判定与句式。
- `references/core-formula.md` —— **八要素万能公式**与组装范例（U 型布局适配）。
- `references/multimodal-referencing.md` —— [图 N] / [视频 N] / [音频 N] 的用法、主体定义、Asset 库注意事项。
- `references/camera-language.md` —— 6 种景别 + 4 种角度 + 6 种基础运镜 + 10 种电影级运镜公式。
- `references/first-person-pov.md` —— **第一人称视角专章**：POV / FPV / Orb 三类子型、六要素骨架、5 套实测 Recipe、Orb 一镜到底动作戏模板、6 大常见错误。
- `references/style-aesthetics.md` —— 动画 / 电影 / 光影色彩 / 视觉效果风格词典。
- `references/text-in-video.md` —— Slogan / 字幕 / 气泡台词 / 时间戳 HUD 文字生成公式。
- `references/video-editing.md` —— 元素增删改 / 向前向后延长 / 轨道补齐（最多 3 段，总 ≤15 秒）/ 延长跳变修复。
- `references/special-chars.md` —— `()` 音乐、`<>` 音效、`{}` 台词、`【】` 字幕；时长单位"秒"；避免符号噪声。
- `references/scene-templates.md` —— 6 大行业（影视、漫剧/短剧、广告营销、在线教育、AIGC 创意、游戏）现成模板。
- `references/pitfalls-visual.md` —— V-1 ~ V-18 视觉类陷阱（含 V-13 九宫格、V-14 时间戳 HUD、V-15 多人歧义、V-16 背景招牌、V-17 数字滚动、V-18 多人物多素材上限）。
- `references/pitfalls-audio.md` —— A-1 ~ A-4 音频类陷阱 + F-1 NSFW（海外）。
- `references/glossary.md` —— 中英对照术语表。
- `assets/prompt-template.md` —— 可填空的 prompt 骨架。
- `assets/examples/` —— 5 个 before / after 改写范例。

## 输出语言

默认中文。用户用什么语言提问就用什么语言回应；prompt 主体仍然用中文撰写（Seedance 中文语义理解最稳）。
